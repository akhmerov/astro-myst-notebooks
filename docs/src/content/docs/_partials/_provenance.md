This paragraph comes from an included file. Select the words **included file**
and inspect `window.mystSourceMap.resolve(window.getSelection().getRangeAt(0))`
in the browser console. The returned origin names the included file, carries
its content digest, and marks the mapping as exact or as a containing range.

Generated output has no authored prose location. Browser edits do not rewrite
the source origins attached to the original document.
